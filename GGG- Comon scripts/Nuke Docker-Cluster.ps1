# kubectl delete deployments --all --all-namespaces
# fuck it for now we just go into docker windows view and see waht images are running
# the script chatgtp gave nukes a bunch of shit that brings the k8s cluster infrastructure